# set

```bash
# Start client locally
cd client/
yarn dev

# Start server locally, using server/dist
cd server/
yarn start

# Build client into server/dist
cd client/
yarn build

# Deploy
cd server/
flyctl deploy

```
